{
  "name": "chitchat-functions",
  "private": true,
  "engines": {"node": "20"},
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "echo lint placeholder"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
